# KeyMacro — 背景遊戲按鍵發送器

## 功能

模仿 X-Mouse Button Control 的概念，允許使用者設定「觸發鍵」與「發送按鍵組合」，並對背景視窗精確發送指令。

## 核心功能

1. **觸發鍵設定** — 指定一個熱鍵（例如 F1）來啟動巨集
2. **發送內容設定** — 可設定：
   - 單鍵（WASD、空白、Enter 等）
   - 組合鍵（Ctrl+C、Shift+A、Win+E 等）
   - 順序發送（先按 A 再按 B 再按 C）
   - 持續時間（每個按鍵按住多久）
   - 發送間隔（每個按鍵之間停多久）
3. **重複次數** — 設定要重複執行幾次
4. **目標視窗** — 可指定視窗名稱或留空（發送給前景視窗）
5. **即時啟動/停止熱鍵** — 用另一個熱鍵隨時中斷

## 操作流程

```
選擇觸發鍵 → 設定發送內容 → 設定頻率/重複 → 選擇目標視窗 → 啟動監聽
```

## 按鍵格式（國際化支援）

使用 `vk` + `scan` 對應表，支援：
- 普通按鍵：a, b, space, enter, up, down, left, right
- 組合鍵：ctrl+c, shift+a, alt+f4, win+d
- 功能鍵：f1~f24, tab, escape, insert, delete
- 數字鍵：0~9, num0~num9
- 符號：!, @, #, $, ^, &, *

## 技術架構

- **Python 3** + **tkinter** GUI
- **pynput** — 監聽全域鍵盤事件（觸發熱鍵）
- **pywin32** — 使用 `PostMessage` + `WM_KEYDOWN/WM_KEYUP` 發送至背景視窗
- **threading** — 非同步執行，不阻塞 GUI

## 國際化

- 介面語言：正體中文（預設）
- 所有說明以繁體中文呈現
